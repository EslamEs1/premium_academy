from django.apps import AppConfig


class PriceConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.price'
    label = 'price'
    verbose_name = 'الأسعار والباقات'
